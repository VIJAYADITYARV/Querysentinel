# QuerySentinel — Week 3 Environment Variables
# Copy this file to .env and fill in your real key
# NEVER commit your actual .env file to GitHub

OPENAI_API_KEY=sk-your-key-here

# Optional — only needed if you didn't fix these already in Week 1
DB_HOST=localhost
DB_PORT=5433
DB_NAME=querysentinel
DB_USER=postgres
DB_PASSWORD=querysentinel
